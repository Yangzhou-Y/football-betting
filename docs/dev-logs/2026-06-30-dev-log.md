# 开发日志 — 2026-06-30

## 项目：FootballBetting DApp 世界杯竞猜平台

> **今日主线**：`fix/frontend-update3` 分支 — 首页统计细化 + 赛事筛选分页 + 事件缓存优化 + Code Review + 球队搜索 Bug 修复 + 双语搜索 & 多页面筛选推广

在 fix/frontend-update2 已合并到 main 的基础上，创立 fix/frontend-update3 分支进行第三轮前端功能增强。覆盖了首页统计卡片细化、赛事列表日期/球队筛选、分页增强、事件缓存性能优化，以及通过 Code Review 发现并修复的多个问题。

---

## 一、事件缓存性能优化（3 个 commit）

### 1.1 BigInt 序列化 Crash 修复（`f48bbe6`）

**问题**：事件缓存使用 `JSON.stringify/parse` 存储到 localStorage，但 `bigint` 类型无法被 `JSON.stringify` 序列化，导致运行时抛出 `TypeError: Do not know how to serialize a BigInt`。

**修复**：在 `eventScanCache.ts` 中为 `JSON.stringify` 提供自定义 `replacer` 函数，将 `bigint` 转换为 `{ __bigint__: "123..." }` 标记对象；`JSON.parse` 时通过 `reviver` 还原。覆盖所有事件扫描结果中的 bigint 字段（poolHome、poolDraw、poolAway、totalPool、minBet、maxBet、amount、reward 等）。

**涉及文件**：`frontend/src/lib/eventScanCache.ts`（新建，+146 行）

### 1.2 增量扫描 + localStorage 缓存（`1e3337d`）

**实现**：
- 新增 `eventScanCache.ts` 模块，将事件扫描结果缓存到 localStorage
- 增量扫描：记录上次扫描到的区块号（`lastScannedBlock`），后续扫描只拉取新区块
- 缓存过期时间：5 分钟（`CACHE_TTL = 300_000`）
- 仅在缓存缺失或过期时重新扫描链上事件

**涉及文件**：`frontend/src/lib/eventScanCache.ts`，`frontend/src/hooks/useLeaderboard.ts`（+93/-30 行），`frontend/src/hooks/useParticipantCounts.ts`（+127/-40 行）

### 1.3 缓存驱逐时清理 checkpoint（`3c3c4a9`）

**问题**：当事件缓存因过期被清除时，`lastScannedBlock` checkpoint 未同步清除，导致下次扫描从旧 checkpoint 开始但缓存为空，数据不一致。

**修复**：在 `evictCache()` 中同时清除对应的 checkpoint key。

**涉及文件**：`frontend/src/lib/eventScanCache.ts`

---

## 二、前端功能增强（3 个 commit）

### 2.1 首页统计细化 + 赛事列表筛选分页（`ce21dec`）

**首页改进**：
- 第一张统计卡片（赛事总数）新增子行：`"共 X 场 · 竞猜中 Y 场 · 已开奖 Z 场"`
- 第三张统计卡片从"投注中数量"改为"竞猜中奖池（USDT 金额）"
- `StatCard` 组件新增可选 `children` prop

**赛事列表改进**：
- 新增日期筛选（HTML date input，UTC+8 时区比较）
- 新增球队名称搜索（case-insensitive 子串匹配）
- 分页增强：新增首页/末页按钮（`«` `»`）、跳转输入框 + 前往按钮
- 页码显示居中，最小宽度 80px 防抖动
- 排序按钮移至筛选行右侧，筛选/日期/搜索分两行布局

**涉及文件**：`frontend/src/app/page.tsx`（+18/-8），`frontend/src/app/matches/page.tsx`（+148/-26），`frontend/src/lib/i18n.tsx`（+18 个 key）

### 2.2 Code Review + 球队搜索 Bug 修复（`108c92f`）

**发现**：Code Review 过程中发现球队搜索完全不可用。

**根因**：`m.homeTeam` / `m.awayTeam` 是合约存储的 bytes32 十六进制字符串（如 `0xE5B7B4E8A5BF...`），搜索框输入的是人类可读的队名（如"巴西"）。直接在 hex 上做 `.includes("巴西")` 永远匹配不到。

**修复**：在比较之前调用项目已有的 `decodeTeamName()` 工具函数解码 hex → 文本。

**涉及文件**：`frontend/src/app/matches/page.tsx`（+4/-2）

### 2.3 双语搜索 + 筛选动画 + MyBets/Admin 筛选推广（`9617b6a`）

**双语球队搜索**：
- 导入 `translateName()` 为每支球队生成 3 个搜索变体：原始名、中文翻译、英文翻译
- 用户输入"Brazil" → 匹配"巴西"；输入"巴" → 匹配"巴西"；输入"bra" → 匹配"Brazil"
- 使用 `[raw, zh, en].some(v => v.includes(query))` 模式

**筛选动画**：
- 在 grid/table 容器的 React `key` 中加入 `filterDate` 和 `filterTeam` 值
- 每次筛选变更 → key 变化 → React 重新挂载 → CSS `animate-page-enter` 动画重放
- 3 个页面统一使用此模式

**MyBets 页面筛选**：
- 新增日期筛选 + 球队搜索 UI（位于统计卡片与投注表格之间）
- 筛选逻辑：在排序后的 `order` 数组上应用 `.filter()`，再分页
- 已删除赛事自动被排除
- 动画 key 更新

**Admin 赛事管理筛选**：
- 新增日期筛选 + 球队搜索 UI（位于表格头部操作栏左侧，与排序按钮同行）
- 筛选逻辑：在 `validMatches` 链式操作中插入 `.filter()` 步骤
- 使用紧凑尺寸（`text-[10px] sm:text-xs`）适配管理表格风格
- 动画 key 更新

**placeholder 国际化**：
- 新增 i18n key `filter.teamPlaceholder`
- zh: `"如 巴西"`，en: `"e.g. Brazil"`
- 随语言切换实时更新

**涉及文件**：`frontend/src/app/matches/page.tsx`，`frontend/src/app/my-bets/page.tsx`（+73），`frontend/src/app/admin/page.tsx`（+63），`frontend/src/lib/i18n.tsx`（+2）

---

## 三、Code Review 发现

在 `ce21dec` 提交上执行了 8 角度 Code Review（correctness × 3 + cleanup × 3 + altitude + conventions），主要发现：

| # | 严重度 | 文件 | 摘要 |
|---|--------|------|------|
| 1 | 🐛 Bug | `matches/page.tsx:134` | "我的投注"早期 return 跳过日期/球队筛选 |
| 2 | ⚡ 性能 | `matches/page.tsx:140` | `filterDateStr` 在 filter 回调内每次迭代重复计算 |
| 3 | 📝 重复 | `matches/page.tsx:304-316` | 跳转验证逻辑在 Enter/Go 按钮中写了两次 |
| 4 | 🔧 健壮性 | `page.tsx:125-128` | `.replace()` 链依赖占位符顺序 |
| 5 | ⚡ 性能 | `matches/page.tsx:164` | `key={address}` 导致钱包切换时全量卸载 |

其中 Finding #1（筛选器组合）和 Finding #2（球队搜索 hex vs 文本）已在本轮修复。

---

## 四、涉及文件（fix/frontend-update3）

| 文件 | 变更 |
|------|------|
| `frontend/src/lib/eventScanCache.ts` | **新建**，事件扫描缓存模块（BigInt 序列化 + localStorage + 增量） |
| `frontend/src/hooks/useLeaderboard.ts` | 集成事件缓存，增量扫描 |
| `frontend/src/hooks/useParticipantCounts.ts` | 集成事件缓存，增量扫描 |
| `frontend/src/hooks/useWaitForTx.ts` | 排除 participantCounts 无效化 |
| `frontend/src/lib/config.ts` | 修复 deployBlock 配置 |
| `frontend/src/app/page.tsx` | 首页统计细化（breakdown + activePool） |
| `frontend/src/app/matches/page.tsx` | 筛选增强 + 分页增强 + 双语搜索 + 动画 |
| `frontend/src/app/my-bets/page.tsx` | 新增日期/球队筛选 |
| `frontend/src/app/admin/page.tsx` | MatchManagement 新增日期/球队筛选 |
| `frontend/src/lib/i18n.tsx` | 新增 20 个翻译 key |
| `scripts/check.ts` | USDT_DECIMALS 修复 |
| `scripts/test-bet.ts` | USDT_DECIMALS 修复 |

---

## 五、Git 提交记录（2026-06-30）

| 提交 | 时间 | 说明 |
|------|------|------|
| `9617b6a` | 18:33 | feat(frontend): bilingual team search + filter animation + filters for MyBets & Admin |
| `108c92f` | 18:13 | fix(frontend): decode bytes32 hex before team name search |
| `ce21dec` | 16:35 | feat(frontend): homepage stats breakdown + matches filter & pagination |
| `3c3c4a9` | 11:41 | fix: clear checkpoint when event cache is evicted to prevent data loss |
| `f48bbe6` | 11:37 | fix: handle BigInt serialization in event caching (CRITICAL BUG) |
| `1e3337d` | 11:08 | perf: add incremental event scanning with localStorage caching |

---

## 六、当前系统状态

### 分支

| 分支 | 状态 |
|------|------|
| `main` | 生产分支，已合并 fix/frontend-update2（`f05e37f`） |
| `fix/frontend-update3` | 当前分支，6 个 commit，已推送远程，待合并 |

### 合约

| 项目 | 值 |
|------|-----|
| 网络 | Conflux eSpace 测试网 (chainId=71) |
| RPC | `https://evmtestnet.confluxrpc.com` |
| 合约地址 | `0x8A60409F40fEDFE7D07D61866757899F2fE35B63` |
| USDT 地址 | `0x7d682e65efc5c13bf4e394b8f376c48e6bae0355` |
| USDT 小数位 | 18 |

### 前端

| 项目 | 值 |
|------|-----|
| 在线地址 | https://football-betting-mu.vercel.app |
| 框架 | Next.js 16.2.9 + React 19.2.4 + wagmi 3.6.16 + RainbowKit 2.2.11 |
| 新增文件 | `lib/eventScanCache.ts`（事件缓存模块，146 行） |
| 语言 | 中/英双语，新增 20 个翻译 key（filter/teamPlaceholder、page/分页增强、stats/统计细化） |

### 本轮统计

| 指标 | 值 |
|------|-----|
| 新增文件 | 1 个（eventScanCache.ts） |
| 修改文件 | 11 个 |
| 总增行数 | +646 |
| 总删行数 | -74 |
| 分支提交数 | 6 个 |
| Bug 修复 | 2 个（BigInt 序列化 Crash、球队搜索 hex 匹配失败） |

---

## 七、经验总结

1. **bytes32 编码的"透明性"陷阱**：合约返回的 `homeTeam`/`awayTeam` 是 bytes32 hex，但 TypeScript 类型是 `string`，IDE 不会给出任何提示。`MatchCard` 组件内部通过 `TeamNameDisplay` 自动解码显示，但筛选逻辑直接使用原始值时就会静默失败。教训：凡是合约返回的 bytes32 字段，在业务逻辑中使用前必须显式调用 `decodeTeamName()`。

2. **双语搜索的搜索空间**：仅解码 hex 不够——如果管理员创建赛事时输入中文队名（如"巴西"），英文用户搜索"Brazil"就无法匹配。需要利用已有的 `translateName()` 双向翻译表，为每支球队生成中/英两个搜索变体。搜索时对 3 个变体（原始 + zh + en）全部检查。

3. **CSS 动画的 React 触发模式**：`animate-page-enter` 通过 CSS `animation` + `nth-child` delay 实现 staggered 入场。React 中触发重放的唯一可靠方式是改变容器的 `key`，使其卸载重建。翻页靠 `safePage`，排序靠 `sortNewest`，筛选靠 `filterDate` + `filterTeam`——所有触发源都编码进 key。

4. **筛选逻辑的链式组合**：在 3 个页面中，筛选的实现模式各不相同——matches 页面在 `allValid.filter()` 中做所有筛选，my-bets 页面在 `order.filter()` 中做筛选（因为数据源是用户投注索引而非完整比赛列表），admin 页面在 `validMatches` 的链式 `.filter().filter().sort()` 中插入筛选步骤。选对插入点很关键——要在排序之前做筛选以保证分页正确。

5. **BigInt 序列化是 Web3 前端的经典坑**：`JSON.stringify` 不支持 `bigint`，而合约返回的几乎所有数值字段（奖池、金额、时间戳）都是 `bigint`。任何涉及 `localStorage` 或网络传输的缓存方案都必须处理序列化。自定义 `replacer/reviver` 是标准解法。

---

## 八、已知限制 / 待办

1. 筛选逻辑在 3 个页面中各自实现，可提取为 `useMatchFilter` hook
2. `filterDateStr` 在 filter 回调内部每次迭代重新计算（性能优化待处理）
3. 分页组件（首页/末页/跳转）仅在 matches 页面有完整版，my-bets 和 admin 仍为简化版
4. `stats.openMatches` 和 `stats.settledCount` i18n key 已成为死代码（被 `stats.matchBreakdown` 替代）
5. `key={address}` 在 matches 和 my-bets 页面导致钱包切换时全量卸载（遗留模式）
6. 事件缓存 TTL 硬编码为 5 分钟，可考虑根据区块产出速度动态调整
