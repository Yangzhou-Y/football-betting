# 开发日志 — 2026-06-16

## 项目：FootballBetting DApp 世界杯竞猜平台

---

## 一、合约层改动（FootballBetting.sol）

### 1.1 允许重复投注 + 修改选项

**问题**：用户每场只能投注一次，无法追加或修改。

**修改**：重写 `placeBet` 函数逻辑。

| 场景 | 旧行为 | 新行为 |
|---|---|---|
| 首次投注 | 正常投注 | 正常投注（不变） |
| 相同选项再投 | `revert: already placed a bet` | 累加金额到已有投注，追加奖池 |
| 切换不同选项 | `revert: already placed a bet` | 旧金额从旧奖池撤出，全部转入新奖池，投注记录更新 |

### 1.2 新增 cancelBet 函数

**问题**：用户投注后无法撤回。

**修改**：在合约 `第十二节` 新增 `cancelBet(uint256 matchId)` 函数。

- 仅 Open 状态 + 截止时间前可调用
- 从对应奖池移除全部投注金额
- `delete bets[matchId][msg.sender]` 删除投注记录
- `usdt.transfer(msg.sender, refundAmount)` 退回 USDT
- 带 `noReentrancy` 和 `whenNotPaused` 防护

### 1.3 修复切换选项时金额计算错误（差额结算）

**问题**：用户已有 10 USDT 押主队胜，切换到客队胜 20 USDT 时：
- 奖池移动正确（旧池 -10，新池 +20）
- 但 `transferFrom` 又全额扣了 20 USDT
- 结果：用户共支付 30 USDT，合约只记录 20，相当于丢了 10 USDT

**修改**：切换选项时改为差额结算：

| 新 vs 旧 | 行为 |
|---|---|
| 新 > 旧 | `transferFrom(msg.sender, address(this), 差额)` 补收 |
| 新 < 旧 | `transfer(msg.sender, 差额)` 退回给用户 |
| 新 == 旧 | 零转账，纯奖池移动 |

### 1.4 新增内部辅助函数

新增 `_addToPool(Match storage m, Result r, uint128 amount)` 和 `_removeFromPool(Match storage m, Result r, uint128 amount)`，内部处理 HomeWin/Draw/AwayWin 三条路径的奖池和 totalPool 增减，避免在 `placeBet`、`cancelBet`、`recordResult` 中重复相同逻辑。

---

## 二、部署脚本（scripts/deploy.ts）

### 2.1 多账户 USDT 发放

**问题**：部署后只有 Account #0（管理员）有 USDT，无法进行多用户测试。

**修改**：在本地链 MockERC20 部署后，循环为前 5 个账户各 mint 100,000 USDT：

```ts
const testUserCount = 5;
for (let i = 0; i < testUserCount; i++) {
    await mockUsdt.mint(signers[i].address, ethers.parseUnits("100000", 6));
}
```

### 2.2 部署记录自动同步到前端目录

**问题**：每次部署后需要手动复制合约地址到 `frontend/src/lib/config.ts`。

**修改**：部署脚本在步骤 6 新增写入操作，将 `{contractAddress, usdtAddress, platformFeeRate}` 同步写入 `frontend/src/lib/deployments/<network>.json`，前端通过 TypeScript `import` 自动读取，部署后刷新页面即生效，不再需要手动改代码。

### 2.3 测试账户信息打印

部署完成后打印所有 5 个测试账户的地址、ETH 余额、USDT 余额、私钥，方便直接导入钱包。

---

## 三、交互脚本

### 3.1 修复 EVM 时间重置函数（interact.ts / fix-time.ts）

**问题**：`interact.ts` 执行时通过 `evm_setNextBlockTimestamp` 跳到截止时间后（约 +30 分钟），脚本结束时 `resetEvmToRealTime()` 试图设回系统时间 → Hardhat 拒绝回退 → 报错。

**修改**：`resetEvmToRealTime()` 和 `fix-time.ts` 均改为 `Math.max(系统时间, 当前区块时间) + 1`，确保永远不向过去设置。

**修复**：`test-create.ts` 中 `block!.timestamp + 3600n` → `block!.timestamp + 3600`，解决 `number + bigint` 类型错误。

---

## 四、前端仓库

### 4.1 新建文件

| 文件 | 用途 |
|---|---|
| `src/hooks/useWaitForTx.ts` | 封装 `useWaitForTransactionReceipt`，交易确认后自动 `queryClient.invalidateQueries()` 刷新所有链上查询 |
| `src/hooks/useCancelBet.ts` | 取消投注 hook，返回 `handleCancel` / `isCancelling` / `isConfirming` / `isCancelled` / `cancelError` |
| `src/lib/errors.ts` | `parseContractError(error)` 将合约 revert 原因（如 `"FootballBetting: contract is paused"`）映射为中文友好提示（如 `"合约已暂停，暂时无法操作"`），覆盖 30 种错误场景 |
| `src/lib/deployments/localhost.json` | 部署记录 JSON 文件，由 deploy.ts 自动写入，前端通过 import 读取 |
| `src/components/layout/PageTransition.tsx` | 页面切换动画组件，路由变化时页面根节点的直接子级卡片逐级延迟下滑（窗帘效果） |
| `src/components/layout/WorldCupBackground.tsx` | 世界杯主题背景组件：绿茵渐变底色（上白下绿）、8 个 SVG 五边形经典足球（带弹跳动画）、2 个球门（门柱+球网）、3 个大力神杯（金色渐变+地球顶+红色飘带+呼吸动画） |

### 4.2 修改文件

| 文件 | 改动内容 |
|---|---|
| **config.ts** | 从 `import localhostData from "./deployments/localhost.json"` 读取地址，删除硬编码的 `DEPLOYMENTS` 手动映射表；`getDeployment` 函数已移除 |
| **utils.ts** | 重写 `encodeTeamName` / `decodeTeamName`：从 viem 的 `stringToHex`/`hexToString` 改为原生 `TextEncoder`/`TextDecoder` byte-by-byte 编解码，解决中文字符显示为乱码的问题 |
| **globals.css** | 新增 `@keyframes pageEnter`（`translateY(-30px)` → `0`，300ms ease-out）；简化 body 背景为绿茵渐变 |
| **layout.tsx** | 引入 `WorldCupBackground` 和 `PageTransition`；`<main>` 内包裹 `<PageTransition>{children}</PageTransition>`；移除 `body` 的 `bg-slate-50` |
| **Navbar.tsx** | ① 钱包按钮旁显示 **USDT 余额**（`0.00 USDT`，绿色粗边框醒目样式）② **角色标识**：管理员 → 红色 `管理员` 标签，用户 → 灰色 `用户` 标签（通过与链上 `owner()` 比对实时判定）③ **← 返回**（`router.back()`）和 **↻ 刷新**（`router.refresh()`）按钮，带 `border-2` 粗边框样式 |
| **usePlaceBet.ts** | ① 新增 `balanceOf` 查询检查 USDT 余额 ② `insufficientBalance` 状态：余额不足时阻止投注按钮 ③ `useWaitForTransactionReceipt` → `useWaitForTxAndRefresh`（交易确认后自动刷新） |
| **useClaimReward.ts** | 暴露 `claimError`；`useWaitForTransactionReceipt` → `useWaitForTxAndRefresh` |
| **admin/page.tsx** | ① 所有管理操作的回执 hook 改为 `useWaitForTxAndRefresh` ② `CreateMatchForm` 错误提示调用 `parseContractError` 转中文 ③ 投注/开放/关闭按钮增加 `disabled={isAdminPending}` 加载态 ④ `RecordBtn` 弹窗 z-index 从 `z-50` → `z-[9999]` ⑤ `ReopenBtn` 组件及"重开"按钮、对应确认弹窗全部删除 ⑥ 结构微调适配新导入 |
| **matches/[id]/page.tsx** | ① 投注按钮**三段式状态**：未输入金额 → 灰色 `输入金额后确认投注`（`cursor-not-allowed`），已输入处理中 → 蓝色半透明，已输入可投 → 蓝色实底 ② 已有投注时显示蓝色提示框（当前投注选项 + 金额）③ 新增 **取消投注** 按钮（红色文字 + 红色边框，紧贴投注按钮下方），仅已有投注时显示 ④ `ClaimPanel` 新增友好错误提示 |

---

## 五、Bug 修复

| # | 现象 | 根因 | 修复文件 | 修复方式 |

| 1 | 创建比赛输入"荷兰""英格兰"后显示乱码 | viem `stringToHex` 对中文编码异常导致存储/读取不一致 | `utils.ts` | 改用 `TextEncoder`/`TextDecoder` 逐字节编码和解码 |

| 2 | 管理后台"暂停"按钮显示错误——合约已暂停但页面显示"运行中" | `PausePanel` 用 `useState(false)` 本地跟踪，刷新丢失 | `admin/page.tsx` | 改为 `useReadContract({functionName: "paused"})` 从链上读取 |

| 3 | 交易完成后页面数据不更新 | 无确认后刷新机制 | 全部写入组件 + hooks | 创建 `useWaitForTxAndRefresh`，`isSuccess` 时 `invalidateQueries` |

| 4 | 用户 USDT 余额不足仍可提交投注交易 | 前端未检查余额 | `usePlaceBet.ts` | 新增 `balanceOf` 查询，不足时按钮显示"USDT 余额不足"并置灰 |

| 5 | 切换投注选项后旧金额消失 | `transferFrom` 全额扣款但旧金额已留在合约中 | `FootballBetting.sol` `placeBet` | 改为差额结算（多退少补） |

| 6 | 录入比分弹窗被导航栏/页面控件遮挡 | 弹窗 `z-50` 与 Navbar 同级 | `admin/page.tsx` | 所有弹窗 z-index 提升至 `z-[9999]` |

| 7 | 运行 `interact.ts` 后 `evm_setNextBlockTimestamp` 报错 | 脚本结束时的 `resetEvmToRealTime` 试图回退时间 | `interact.ts`、`fix-time.ts` | 改用 `Math.max(realTime, blockTime) + 1` |

| 8 | MetaMask 持续报 `Failed to connect` | MetaMask 注册未完成导致扩展半激活 + 双钱包冲突 | 环境侧 | 用恢复助记词方式跳过 MetaMask 注册；禁用未完成注册的 MetaMask 扩展，单独使用 Rabby |

| 9 | MetaMask 导入私钥报错 | 粘贴的私钥带 `0x` 前缀，MetaMask 不识别 | 环境侧 | 去掉 `0x` 前缀重新导入 |

| 10 | Rabby 导入 mUSDT 后余额显示 1100 亿 | Rabby 自动识别小数位失败，默认 0 位 | 环境侧 | 改为在 Navbar 显示 USDT 余额（前端直接读合约），不经钱包 |

| 11 | `eth_call` 报 `unrecognized-selector` + 交易 `Nonce too high` | 合约未重新部署 + 钱包 nonce 不同步 | 环境侧 | `npm run node` → `npm run deploy` → 钱包 Reset Nonce |

---

## 六、测试环境

### 6.1 测试账户

| # | 角色 | 地址 | 私钥 | 初始 ETH | 初始 USDT |

| 0 | 管理员 | `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` | `0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80` | 10,000 | 100,000 |

| 1 | 用户 | `0x70997970C51812dc3A010C7d01b50e0d17dc79C8` | `0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d` | 10,000 | 100,000 |

| 2 | 用户 | `0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC` | `0x5de4111afa1a4b94908f83103eb1f1706367c2e68ca870fc3fb9a804cdab365a` | 10,000 | 100,000 |

| 3 | 用户 | `0x90F79bf6EB2c4f870365E785982E1f101E93b906` | `0x7c852118294e51e653712a81e05800f419141751be58f605c371e15141b007a6` | 10,000 | 100,000 |

| 4 | 用户 | `0x15d34AAf54267DB7D7c367839AAf71A00a2C6A65` | `0x47e179ec197488593b187f80a00eb0da91f1b9d0b13f8733639f19c30a34926a` | 10,000 | 100,000 |

### 6.2 部署快捷指令

```bash
# 终端 A — 启动 Hardhat 本地节点（启动本地链）
npm run node

# 终端 B — 编译 + 部署合约 + 自动同步前端地址
npm run deploy

# 终端 B — 运行交互演示
npm run interact

# 终端 C — 启动前端开发服务器（只需启动一次）
npm run dev

# 可选 — 运行完整交互演示
npx hardhat run scripts/interact.ts --network localhost

# 可选 — 修复 EVM 时间偏移
npx hardhat run scripts/fix-time.ts --network localhost
```

### 6.3 钱包 Nonce 重置方法

| 钱包 | 操作路径 |
|---|---|
| **MetaMask** | 设置 → 高级 → 清除活动和非ce数据 |
| **Rabby** | 点击账户名 → 右侧 `···` → Reset Nonce |

---

## 七、未完成事项 / 已知限制

1. MetaMask 扩展注册在中国大陆网络环境下不稳定，需 VPN 或使用 Rabby 替代👌
2. 合约 `reopenMatch` 函数保留在合约中，但前端管理后台已移除对应按钮，如需恢复需重新添加 UI👌
3. 钱包地址备注（MetaMask/Rabby 本地标签）无法通过 Web3 API 读取，Navbar 中显示的账户名仍为缩短地址格式
4. 页面切换动画在管理后台按卡片层级延迟，其他页面（首页/赛事/排行榜）受各自 DOM 结构影响效果可能不完全一致👌
5. Hardhat 节点重启后所有链上数据归零，需重新 mint USDT
6. 录入成绩ui位置仍存在问题👌
7. 多用户测试未进行👌
8. 管理员单用户测试未完全完成👌
9. 刷新按钮不起作用

## 八、拓展性提问

1. 目前是将 agent 用作 vibe coding，相当于把大模型嵌入到编译器里为我编写代码。agent 有没有除了写代码外的其它用途？即不是放在编译器中而是在其他地方应用