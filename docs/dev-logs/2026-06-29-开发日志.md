# 开发日志 — 2026-06-29

## 项目：FootballBetting DApp 世界杯竞猜平台

> **今日主线**：`fix/frontend-update1` 合并 → 创建 `fix/frontend-update2` → 6 项前端改进 → 3 次 Bug 修复 → 排序优化 → 合并 main

在 fix/frontend-update1 已合并的基础上，针对项目经理审计中发现的 gap 以及用户体验反馈，创立 fix/frontend-update2 分支进行第二轮前端功能增强。Cover 了排行榜访问权限、一键领奖、倒计时、快捷金额联动、骨架屏、扫描进度、排序切换等多项改进。

---

## 一、`fix/frontend-update1` 合并

### 1.1 合并到 main

将 `fix/frontend-update1` 分支（18 个 commit）fast-forward 合并到 main：

```
5cfd7e7 fix: label each odds with result type (home/draw/away) on match cards
...
f573ff7 docs: 为整个项目添加详细完整的中文注释（31 文件 / 884 行）
```

包含内容：
- 前端 UX 增强（赔率标注、比赛时间、分页翻页、动画）
- 移动端 UI 修复（datetime 等宽、表格字号、翻页滚动定位）
- 配置修复（未连接钱包回退到测试网，`chain?.id ?? 71`）
- i18n 扩展（世界杯 1/16、1/8 决赛）
- 开发日志

合并后 `main` 在 `5cfd7e7`，切回 `fix/frontend-update1` 继续工作。

---

## 二、创建 `fix/frontend-update2` 分支

基于 `fix/frontend-update1` 创建新分支 `fix/frontend-update2`，实施以下 6 项改进。

---

## 三、六大前端改进（`951d5c8`）

### 3.1 未连接钱包也可查看排行榜

**问题**：排行榜页面在未连接钱包时显示"请先连接钱包"，用户必须安装 MetaMask 才能看到排名。

**修复**：移除 `leaderboard/page.tsx` 中 `!isConnected` 的提前返回守卫。`useLeaderboard` hook 使用 `usePublicClient()`（wagmi 内置 public RPC），无需钱包签名即可扫描链上事件。

**涉及文件**：`frontend/src/app/leaderboard/page.tsx`（删除 ~8 行）

### 3.2 一键领取奖励

**问题**：用户赢了多场比赛后，需要逐场进入详情页点击领取，操作繁琐。

**实现**：

1. **`ClaimButton` 组件**：基于 `usePreviewReward(matchId)` 显示合约实时计算的待领金额（而非存储中始终为 0 的 `rewards` 字段）。每行未领取的中奖比赛直接显示领取按钮。

2. **Claim All 横幅**：页面顶部自动汇总所有已结算 + 猜中 + 未领取的比赛，显示"可领取奖励"提示和每场比赛的独立领取按钮。判断逻辑为 `!claimed && match.settled && betOn === match.result`（不依赖 rewards > 0）。

**涉及文件**：`frontend/src/app/my-bets/page.tsx`（+83 行，新增 ClaimButton 组件 + Claim All 横幅），`frontend/src/lib/i18n.tsx`（新增 `myBets.claimableTotal` / `myBets.claimableHint` 翻译）

### 3.3 赛事卡片倒计时

**问题**：用户无法直观感知"还有多久截止投注"，容易错过投注窗口。

**实现**：`MatchCard` 组件新增实时倒计时：
- 使用 `useState` + `useEffect` + `setInterval(1000)` 每秒更新
- 仅对 `status === Open` 且 `deadline > 0` 的比赛显示
- 格式：`Xh Xm Xs`（≥1 小时）或 `Xm Xs`（<1 小时）
- 显示为琥珀色 `⏳ 2h 34m 12s`，持续递减至 0
- 倒计时归零后自动消失，切换回常规"投注"状态

**涉及文件**：`frontend/src/components/match/MatchCard.tsx`（+22 行）

### 3.4 快捷金额与 min/max 联动

**问题**：`1` / `10` / `100` 快捷按钮无视比赛的最低/最高投注限制，用户点击后提交可能被合约拒绝。

**实现**：
- 引入 `viem` 的 `parseUnits` 将快捷金额转为 wei 对比 `minBet` / `maxBet`
- 低于 `minBet` 或高于 `maxBet`（且 `maxBet > 0`）的按钮自动禁用（`opacity-30` + `disabled`）
- 禁用时显示 tooltip 提示原因（"低于最低投注额" / "超过最高投注额"）

**涉及文件**：`frontend/src/app/matches/[id]/page.tsx`（+22 行），`frontend/src/lib/i18n.tsx`（新增 `bet.belowMin` / `bet.aboveMax` 翻译）

### 3.5 Loading 骨架屏

**问题**：项目所有 loading 状态都是"加载中..."文字，既单调又无法预判布局，体验较差。

**实现**：新建 `Skeleton` 组件库（`frontend/src/components/shared/Skeleton.tsx`），包含：

| 组件 | 用途 |
|------|------|
| `Skeleton` | 基础灰色脉冲块（`animate-pulse bg-slate-200`） |
| `MatchCardSkeleton` | 单个赛事卡片骨架（状态/队名/奖池/比例条占位） |
| `MatchCardGridSkeleton` | 卡片网格骨架（可配置数量，默认 6） |
| `TableSkeleton` | 完整表格骨架（表头 + N 行，可配置行列） |
| `CardListSkeleton` | 移动端卡片列表骨架（可配置数量） |

**替换范围**：首页、赛事列表、我的竞猜、排行榜 4 个页面的 loading 状态全部从 spinner 文字替换为骨架屏。

**新增文件**：`frontend/src/components/shared/Skeleton.tsx`（+106 行）

### 3.6 排行榜扫描进度条

**问题**：`useLeaderboard` 扫描链上事件时（数千个区块分片拉取），用户看到"正在扫描链上数据..." spinner 但不知道还要等多久。

**实现**：
- `useLeaderboard` hook 新增 `ScanProgress` 接口（`{ current, total }`），在逐片拉取循环中实时更新
- `leaderboard/page.tsx` 在 loading 状态渲染进度条：`(3/14)` + 蓝色进度条 + 骨架屏
- 进度条使用 `transition-all duration-300` 平滑过渡

**涉及文件**：`frontend/src/hooks/useLeaderboard.ts`（+22 行），`frontend/src/app/leaderboard/page.tsx`（+12 行），`frontend/src/lib/i18n.tsx`（新增 `leaderboard.scanning` 翻译）

---

## 四、Bug 修复

### 4.1 React Error #300 — Hook 调用顺序不一致（`1198abf`）

**现象**：投注成功后页面崩溃，显示 `Minified React error #300`。

**根因**：`BettingPanelInner` 组件中，`useMemo` 放在了 JSX 内部（`<div>{useMemo(() => ...)}</div>`）。投注成功后 `isBetSuccess` 变为 `true`，组件提前 `return` 成功消息，`useMemo` 未被调用 → 与正常渲染时 hooks 数量不一致 → React 抛出 #300。

**修复**：将 `useMemo` 提取为组件顶级 `const quickAmounts = useMemo(...)`，放在所有 `useEffect` 之后、`if (isBetSuccess) return` 之前，确保每次渲染都按相同顺序调用。

**涉及文件**：`frontend/src/app/matches/[id]/page.tsx`

### 4.2 排行榜骨架屏卡死（`357fc38` + `45ad82b`）

**现象**：首次打开排行榜（未连接钱包），骨架屏一直显示，数据永远不加载。

**根因分析**：
1. `useBlockNumber()` 在首次渲染时返回 `undefined`，导致 leaderboard query 的 `enabled` 条件不满足
2. Query 被禁用 → `isLoading: false` → 页面跳过 loading 直接显示空数据
3. 加入 `!currentBlock` 到 `isLoading` 后，骨架屏正确显示，但 public client 指向 Hardhat local（`http://127.0.0.1:8545`），生产环境该 RPC 不存在 → `useBlockNumber()` 永久挂起

**修复**：
- 第一步：`useLeaderboard` 返回的 `isLoading` 加入 `|| !currentBlock` 条件，保证区块号未拿到前显示骨架屏
- 第二步：`wagmi.ts` 中 `supportedChains` 顺序从 `[hardhatLocal, confluxTestnet]` 改为 `[confluxTestnet, hardhatLocal]`，使未连接钱包时 public client 默认指向公开的 Conflux 测试网 RPC，而非本地 localhost

**涉及文件**：`frontend/src/hooks/useLeaderboard.ts`，`frontend/src/lib/wagmi.ts`

### 4.3 我的竞猜 Claim All 不显示 / 按钮显示 0.00（`329a8e7`）

**现象**：Claim All 横幅从不出现，单个领取按钮显示"领取奖励 0.00"。

**根因**：
- `getUserAllBets` 返回的 `rewards[i]` 在用户实际调用 `claimReward` 之前始终为 `0n`（奖励金额在领取时才计算并写入存储）
- Claim All 判断条件 `!claimed[i] && rewards[i] > 0n` 永远不成立
- 按钮展示的是存储中的 `rewards[i]`，而非实际待领金额

**修复**：
- `ClaimButton` 改为使用 `usePreviewReward(matchId)` 获取合约实时计算的待领金额
- Claim All 判断条件改为 `!claimed[i] && match.settled && betOns[i] === match.result`（已结算 + 猜中 + 未领取）

**涉及文件**：`frontend/src/app/my-bets/page.tsx`

---

## 五、排序功能优化

### 5.1 三页面排序切换（`eb1b2ec`）

为以下三个页面添加"最近优先" / "最早优先"排序切换按钮：

| 页面 | 按钮位置 | 默认排序 |
|------|---------|---------|
| 赛事列表 | 筛选按钮行右侧 | 最近优先（desc） |
| 我的竞猜 | 标题行右侧 | 最近优先（desc） |
| 赛事管理 | 表格顶部操作栏 | 最近优先（desc） |

实现方式：各页面新增 `sortNewest` 状态，排序逻辑改为 `sortNewest ? -cmp : cmp`，切换时自动回到第一页。

### 5.2 排序动画（`2562421` + `3b76284`）

排序切换时，将 `sortNewest` 编入 React `key`，触发与翻页同效果的 `animate-page-enter` 下滑淡入动画。覆盖我的竞猜、赛事管理、赛事列表三个页面。

### 5.3 纯时间排序（`f05e37f`）

应要求移除赛事列表的"已投注优先"排序逻辑，改为完全按开赛时间排序（仅保留 `sortNewest` 方向控制）。

---

## 六、文档审查

检查 README、CLAUDE.md、AGENTS.md 等文档是否需要更新：

| 检查项 | 结论 |
|--------|------|
| README.md | 只描述合约/部署/架构，不涉及 UI 细节，无需更新 |
| CLAUDE.md / AGENTS.md | 通用规则，无项目特定内容，无需更新 |
| 代码注释 | 所有文件头注释描述页面结构而非实现细节，仍然准确 |
| 开发日志 | `2026-06-26-开发日志.md` 中快捷金额描述为当天状态，后续改动已记录在今日日志 |

---

## 七、涉及文件（fix/frontend-update2）

| 文件 | 变更 |
|------|------|
| `frontend/src/components/shared/Skeleton.tsx` | **新建**，骨架屏组件库（5 个组件，106 行） |
| `frontend/src/app/leaderboard/page.tsx` | 移除钱包守卫 + 进度条 + 骨架屏 |
| `frontend/src/app/matches/page.tsx` | 排序切换 + 排序动画 + 纯时间排序 + 骨架屏 |
| `frontend/src/app/matches/[id]/page.tsx` | 快捷金额 min/max 联动 |
| `frontend/src/app/my-bets/page.tsx` | ClaimButton + Claim All 横幅 + 排序切换 + 排序动画 + 骨架屏 |
| `frontend/src/app/page.tsx` | 骨架屏替换 spinner |
| `frontend/src/components/match/MatchCard.tsx` | 倒计时 |
| `frontend/src/hooks/useLeaderboard.ts` | 扫描进度 + 修复 isLoading 条件 |
| `frontend/src/lib/i18n.tsx` | 新增 8 个翻译 key（claim/sort/scanning/belowMin/aboveMax） |
| `frontend/src/lib/wagmi.ts` | 默认链调整为 Conflux 测试网 |

---

## 八、Git 提交记录（2026-06-29）

| 提交 | 时间 | 说明 |
|------|------|------|
| `f05e37f` | 15:52 | fix: sort matches purely by startTime, remove my-bets-first grouping |
| `3b76284` | 15:48 | feat: trigger page-enter animation when switching sort order on matches page |
| `2562421` | 15:44 | feat: trigger page-enter animation when switching sort order on my-bets and admin |
| `eb1b2ec` | 15:38 | feat: add sort order toggle (newest/oldest first) to matches, my-bets, and admin pages |
| `1198abf` | 15:32 | fix: move useMemo above early returns to prevent React hook ordering error (#300) |
| `329a8e7` | 15:25 | fix: use previewReward for claimable amount, detect won+unclaimed for Claim All banner |
| `45ad82b` | 15:18 | fix: leaderboard stuck on skeleton — public client defaulted to localhost RPC |
| `357fc38` | 15:12 | fix: leaderboard shows 'no data' before block number resolves on first load |
| `951d5c8` | 15:05 | feat: leaderboard access without wallet, claim-all, countdown, quick-amount validation, skeletons, scan progress |

---

## 九、当前系统状态

### 分支

| 分支 | 状态 |
|------|------|
| `main` | 生产分支，已合并 fix/frontend-update1 + fix/frontend-update2（`f05e37f`） |
| `fix/frontend-update2` | 当前分支，9 个 commit，已推送远程，已合并到 main |
| `fix/frontend-update1` | 已合并到 main |

### 合约

| 项目 | 值 |
|------|-----|
| 网络 | Conflux eSpace 测试网 (chainId=71) |
| RPC | `https://evmtestnet.confluxrpc.com` |
| 合约地址 | `0x8A60409F40fEDFE7D07D61866757899F2fE35B63` |
| USDT 地址 | `0x7d682e65efc5c13bf4e394b8f376c48e6bae0355` |
| USDT 小数位 | 18 |

### 前端

| 项目 | 值 |
|------|-----|
| 在线地址 | https://football-betting-mu.vercel.app |
| 框架 | Next.js 16.2.9 + React 19.2.4 + wagmi 3.6.16 + RainbowKit 2.2.11 |
| 新增文件 | `components/shared/Skeleton.tsx`（骨架屏组件库） |
| 语言 | 中/英双语，新增 8 个翻译 key |

### 本轮统计

| 指标 | 值 |
|------|-----|
| 新增文件 | 1 个（Skeleton.tsx） |
| 修改文件 | 10 个 |
| 总增行数 | +361 |
| 总删行数 | -55 |
| 分支提交数 | 9 个 |
| Bug 修复 | 3 个（React #300、骨架屏卡死、Claim All 显示 0.00） |

---

## 十、经验总结

1. **Hooks 不能在 JSX 中调用**：`useMemo` / `useEffect` 等必须放在组件函数体的顶级（所有提前 return 之前），否则在不同渲染路径中 hooks 数量不一致会触发 React error #300。这个坑在之前的 `matches/page.tsx` 翻页动画中也踩过一次。

2. **合约存储字段 ≠ 前端展示字段**：`getUserAllBets` 返回的 `rewards[i]` 在领取前始终为 0，因为 Parimutuel 奖励在 `claimReward` 调用时才计算。前端必须用 `previewReward` 获取实时预览值。这个设计差异导致 Claim All 的第一版实现完全无效。

3. **未连接钱包的 public client 链号**：wagmi 默认取 `supportedChains` 第一个链作为 public client 的目标。如果第一个是本地链（Hardhat），生产环境 RPC 不可达会导致 `useBlockNumber()` 永久挂起。解决方法是把公开 RPC 的链放在数组第一位。

4. **骨架屏 vs Spinner**：骨架屏减少用户对等待时间的感知，但实现成本高于 spinner。本项目用 `animate-pulse` + CSS `bg-slate-200` 即可实现，无需额外依赖。关键是每个页面的骨架屏要匹配真实布局（卡片网格 / 表格 / 统计卡）。

5. **排序功能的一致性**：三个页面（赛事列表、我的竞猜、赛事管理）都需要排序切换，实现模式完全一致 — `sortNewest` 状态 + `cmp` 符号翻转 + key 编码触发动画。这个模式可以提取为可复用 hook，留待后续优化。

---

## 十一、已知限制 / 待办

1. 排序切换逻辑在三个页面中重复实现（`sortNewest` 状态 + `cmp` 翻转），可提取为 `useSortOrder` hook
2. `useMemo` 在 `BettingPanelInner` 中用于快捷金额计算，可进一步提取为独立 hook
3. 骨架屏暂未应用于管理后台页面
4. 倒计时目前显示至 deadline，可考虑同时显示距开赛时间的倒计时
