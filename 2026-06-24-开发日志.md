# 开发日志 — 2026-06-24

## 项目：FootballBetting DApp 世界杯竞猜平台

> **最终成果**：代码侧钱包连接已做到极致（零警告、零报错）。
> - ✅ MetaMask 拓展连接 + 严格检测防伪装（`_metamask` 三级检测）
> - ✅ Rabby 拓展连接
> - ✅ RPC URL 修复: `/rpc/conflux-testnet` → `https://evmtestnet.confluxrpc.com`
> - ⚠️ WalletConnect 桌面端扫码：MetaMask App 内置 WC 为**实验性功能**，偶发不稳定（需多次尝试或等待 relay 同步）。手机端深链跳转正常。
> - 推荐桌面端安装 MetaMask 拓展走 `injected` 连接（100% 稳定），或手机端用 MetaMask App 内置浏览器访问

---

## 一、移动端抽屉遮挡修复

### 1.1 Portal 逃逸层叠上下文

**问题**：iOS Safari 上 `backdrop-filter: blur()` 会创建新的 containing block，导致 `position: fixed` 的移动端抽屉被首页 stat card 和 match filter 控件遮挡。

**根因**：`<nav className="backdrop-blur">` 在 iOS Safari 中形成新层叠上下文，子元素 `z-index` 被限制在 nav 内部，无法覆盖页面其他区域。

**修复** (`a85c664`): 使用 `createPortal` 将抽屉渲染到 `document.body`，彻底脱离 nav 层叠上下文。

```tsx
// Navbar.tsx — 手机端抽屉
{mounted && menuOpen && createPortal(
  <div className="fixed inset-0 z-[9999] sm:hidden">
    <div className="absolute inset-0 bg-black/50" onClick={close} />
    <div className="absolute right-0 top-0 h-full w-64 bg-white shadow-2xl">
      {/* 导航链接 */}
    </div>
  </div>,
  document.body
)}
```

### 1.2 抽屉交互优化

- SPA 路由变化时自动关闭（`useEffect` 监听 `pathname`）
- 打开时禁止 body 滚动（`overflow: hidden`）
- 清理函数恢复 body 滚动

---

## 二、钱包连接 — 移动端 & 桌面端适配

### 2.1 问题背景

项目部署在 Vercel，后端 Conflux eSpace 测试网 (chainId=71)。需支持：
- **桌面端**：MetaMask 拓展、Rabby 拓展、WalletConnect 扫码
- **手机端**：MetaMask App 内置浏览器、Safari/Chrome + WalletConnect 唤起 App

### 2.2 wagmi 版本降级 (v3 → v2)

**问题**：项目使用 wagmi `^3.6.16`，但 RainbowKit v2.2.11 peer dependency 要求 wagmi `^2.9.0`。构建时报 `gemini` connector 未找到。

**修复** (`d244c13`):

| 包 | 旧版本 | 新版本 |
|---|---|---|
| wagmi | `^3.6.16` | `^2.19.5` |
| `@rainbow-me/rainbowkit` | `^2.2.11` | 不变 |
| viem | `^2.52.2` | 不变 |

API 变化：`createWagmiConfig` → `createConfig`，`http(undefined)` → `http()`，需显式设置 `ssr: true`。

### 2.3 SSR 水合错误修复

**问题** (`4d54e6b`): 项目用 `mounted` 守卫跳过 `WagmiProvider` 的 SSR 渲染，但子组件在服务端调用 `useConfig` 时抛出 `WagmiProviderNotFoundError`。

**修复**: 无条件渲染 `WagmiProvider`，`createConfig` 设置 `ssr: true`，让 wagmi 在服务端输出占位数据。

### 2.4 MetaMask 手机端检测

**问题**：RainbowKit 默认在移动端过滤掉所有 injected wallet（认为手机浏览器无拓展），即使 MetaMask App 内置浏览器注入了 `window.ethereum`。

**尝试过的方案**：

| 方案 | 结果 |
|------|------|
| RainbowKit `wallets` prop | v2.2.11 不支持该 prop，TypeScript 报错 |
| 直接加 `injected()` 到 `createConfig` | 移动端 RainbowKit modal 不展示 |
| `getDefaultWallets` 5 钱包 | 桌面端仅 Base 可用，3 个白屏 |
| `connectorsForWallets` + 自定义钱包 | ✅ 可行，移动端出现 MetaMask 按钮 |
| RainbowKit 内置 `metaMaskWallet` | 需 `@metamask/sdk`（已废弃），点击无反应 |

**最终方案**: `connectorsForWallets` 搭配自定义钱包定义，保留 `rkDetails` 元数据让 RainbowKit 在各平台展示。

### 2.5 MetaMask 图标加载

**问题**：自定义钱包的 `iconUrl` 使用动态 import RainbowKit 内置 SVG，运行时加载失败。

**尝试**：

| 方案 | 结果 |
|------|------|
| `import("@rainbow-me/rainbowkit/wallets")` | 运行时失败 |
| URL-encoded SVG data URL | 桌面端仍不显示 |
| **base64-encoded SVG** | ✅ 正常加载 |

最终: `iconUrl: "data:image/svg+xml;base64,PHN2ZyB4bWxucz0i..."` 常量化在代码中。

### 2.6 MetaMask 拓展检测精准化

**问题** (`ea118a2`): Rabby、OKX、Coinbase、Brave、Trust 等钱包均设 `window.ethereum.isMetaMask = true` 来兼容 dapp。单纯判断 `isMetaMask` 会导致 MetaMask 按钮"伪装已安装"——点击后一直显示"正在打开 MetaMask..."但无响应。

**三级优先级检测**：

| 优先级 | 检测方式 | 可靠性 | 说明 |
|--------|----------|--------|------|
| 1 | `window.ethereum._metamask` | 最高 | MetaMask 独有内部 API，其他钱包不设 |
| 2 | `window.ethereum.providers[]` (EIP-6963) | 高 | 逐 provider 检查 `_metamask` 或 `isMetaMask && !isRabby && !isCoinbaseWallet && !isOKExWallet` |
| 3 | `isMetaMask && !isRabby && !isCoinbaseWallet && !isOKExWallet && !isBraveWallet && !isTrust` | 中 | 单 provider 环境，排除 5 个已知伪装者 |

```typescript
function isMetaMaskInstalled() {
  if (typeof window === "undefined") return false;
  const eth = (window as any).ethereum;
  if (!eth) return false;
  if (eth._metamask) return true;
  if (eth.providers?.length) {
    return eth.providers.some(
      (p: any) => p._metamask || (p.isMetaMask && !p.isRabby && !p.isCoinbaseWallet && !p.isOKExWallet)
    );
  }
  return eth.isMetaMask && !eth.isRabby && !eth.isCoinbaseWallet && !eth.isOKExWallet && !eth.isBraveWallet && !eth.isTrust;
}
```

### 2.7 WalletConnect — 桌面端白屏问题（核心难点）

#### 症结

桌面端点击 WalletConnect 按钮后网页白屏（"This page couldn't load"），手机端 WalletConnect 正常工作。

根因链条：
```
RainbowKit v2.2.11 内部 WalletConnect QR 码弹窗组件
  └─ @reown/appkit@1.7.8（WalletConnect 官方弹窗 SDK）
       └─ 均基于 React 18 API 构建
            └─ Next.js 16 强制使用 React 19
                 └─ 弹窗组件崩溃 → 白屏
```

RainbowKit v2 已停止更新（v3 不存在），其内部 WalletConnect 弹窗在 React 19 下不兼容。仅有 Base wallet（Coinbase，使用 `window.open` 独立窗口）不受影响。

#### 尝试方案

| 方案 | 桌面表现 | 手机表现 | 状态同步 |
|------|----------|----------|----------|
| RainbowKit `walletConnectWallet`（裸引用） | 白屏 ✗ | 深链跳转 ✓ | ✓ |
| `connectorsForWallets` + 自定义 + `showQrModal: false` | 弹窗出现但 QR 码/打开按钮无效 ✗ | — | — |
| `connectorsForWallets` + 自定义 + `showQrModal: true` | wagmi 原生弹窗，不白屏 ✓ | 正常 ✓ | ✗ |
| `getDefaultConfig`（官方推荐） | 除 Base 外全白屏 ✗ | 正常 ✓ | ✓ |
| 独立 wagmi connector + 手动 `rkDetails` | 按钮不出现在 modal ✗ | — | — |
| 独立 connector + `Object.assign` 展开元数据 | 按钮不出现在 modal ✗ | — | — |

`showQrModal: true` 让 wagmi 使用自己的 `@walletconnect/ethereum-provider` 原生弹窗（React 无关），绕过 RainbowKit 的 QR 码弹窗 → 不白屏。但 `connectorsForWallets` 包装可能干扰 connector 事件链 → 状态同步丢失 → 无断开按钮、签名不工作。

`showQrModal: false` 让 RainbowKit 渲染自己的 QR 码弹窗 → 弹窗崩溃白屏。

独立 connector 方案无解：RainbowKit v2.2.11 的 wallet modal 只展示通过 `connectorsForWallets` 生成的钱包，外部 connector 即使手动展开元数据也不会出现。

#### 当前方案

回到 `connectorsForWallets` + `showQrModal: true` 方案：

- **MetaMask** — 自定义钱包，严格三级检测，`installed` 标记准确反映拓展存在
- **Rabby** — RainbowKit 内置 `rabbyWallet`
- **WalletConnect** — `connectorsForWallets` 包装 + `walletConnect({ showQrModal: true })`

```typescript
function customWalletConnect() {
  return {
    id: "walletConnect",
    name: "WalletConnect",
    iconBackground: "#3b99fc",
    iconAccent: "#3b99fc",
    iconUrl: WC_ICON,
    createConnector: (walletDetails: any): CreateConnectorFn => {
      return (config: any) => ({
        ...walletConnect({
          projectId,
          showQrModal: true,
          metadata: { name: "Football Betting", ... },
        })(config),
        ...walletDetails,
      });
    },
  };
}
```

---

## 三、RPC URL 修复 — 根因发现

### 3.1 问题

MetaMask App 扫码授权后浏览器没有连上，F12 控制台报错：

```
Error: Connection request reset. Please try again.
Invalid chainId
Expected an array with at least one valid string HTTPS url 'rpcUrls', 
Received: /rpc/conflux-testnet
```

### 3.2 故障链分析

```
WalletConnect 扫码配对 ✓
  → wagmi 发送 wallet_switchEthereumChain 到 MetaMask App
    → 链配置中 rpcUrls: ["/rpc/conflux-testnet"]
      → MetaMask 验证：相对路径不是合法 HTTPS URL
        → 拒绝: "Expected valid HTTPS url"
          → 切链失败 "Invalid chainId"
            → 连接中断 "Connection request reset"
              → 刷新后重连到残废会话
                → 无断开按钮、无法签名
```

### 3.3 修复

`/rpc/conflux-testnet` 是 Next.js API 路由代理，浏览器端可用（同源请求），但 WalletConnect 会将链配置直接发给 MetaMask App，后者无法访问。

**修改** (`7464e3c`):

```typescript
// 旧：相对路径
rpcUrls: {
  default: { http: ["/rpc/conflux-testnet"] },
}

// 新：Conflux 公共 RPC
rpcUrls: {
  default: { http: ["https://evmtestnet.confluxrpc.com"] },
}
```

**影响的修复**（已全部验证通过 ✅）：
- ✅ WalletConnect 切链成功 — MetaMask App 可识别链
- ✅ 连接流程完整完成 — 不再 "Connection request reset"
- ✅ wagmi 状态同步正常 — 桌面端连接后有断开连接按钮
- ✅ 钱包签名操作正常 — 投注、领奖等需要签字的操作均可正常执行
- ✅ 手机端 WalletConnect 正常 — 深链跳转 + 签字

---

## 四、依赖变更

| 操作 | 包 | 说明 |
|------|-----|------|
| 降级 | `wagmi` `^3.6.16` → `^2.19.5` | 匹配 RainbowKit v2 peer dependency |
| 安装后删除 | `@metamask/connect-evm` | MetaMask SDK 连接器，不兼容 |
| 安装后删除 | `@metamask/sdk` `0.33.1` | 已废弃，桌面/手机表现不一致 |
| 保持 | `@rainbow-me/rainbowkit` `^2.2.11` | 最新 v2，无 v3 |
| 保持 | `viem` `^2.52.2` | — |
| 保持 | `next` `16.2.9` / `react` `19.2.4` | Next 16 强制 React 19 |

---

## 五、涉及文件

| 文件 | 变更摘要 |
|------|----------|
| `frontend/src/providers/Web3Provider.tsx` | 核心变更：钱包配置、connectorsForWallets、自定义 MetaMask/WalletConnect 钱包定义、SSR 修复 |
| `frontend/src/components/layout/Navbar.tsx` | Portal 抽屉、`createPortal` import |
| `frontend/src/lib/wagmi.ts` | RPC URL 改为公共地址、移除 `ENABLE_LOCAL` 和 `confluxMainnet` |
| `frontend/package.json` | wagmi 降级、MetaMask 包安装/卸载 |

---

## 六、Git 提交记录（fix/mobile-WalletConnect 分支）

| 提交 | 时间 | 说明 |
|------|------|------|
| `7464e3c` | 18:56 | **fix: use public Conflux eSpace Testnet RPC URL instead of relative proxy path** |
| `5a6b71f` | 18:48 | fix: WalletConnect back in connectorsForWallets so button appears |
| `4232c36` | 18:42 | fix: spread wallet metadata directly on connector instead of nested rkDetails |
| `4b0bab4` | 18:29 | fix: try showQrModal=false with unwrapped connector |
| `a62a891` | 18:13 | fix: WalletConnect as unwrapped wagmi connector with manual rkDetails |
| `a036a1c` | 18:05 | fix: use RainbowKit getDefaultConfig — official recommended setup |
| `246c95a` | 18:03 | Revert "fix: revert to bare walletConnectWallet" |
| `8bdaef6` | 17:58 | fix: revert to bare walletConnectWallet |
| `435a7eb` | 17:47 | fix: use wagmi native WalletConnect modal (showQrModal=true) |
| `a607b2a` | 17:36 | fix: restore RainbowKit walletConnectWallet for proper QR code integration |
| `e8d645a` | 17:30 | fix: remove duplicate WalletConnect connector to eliminate init warnings |
| `ea118a2` | 17:26 | **fix: use MetaMask-specific _metamask API for reliable detection** |
| `d58bfa4` | 17:21 | fix: smart MetaMask wallet — extension or QR code fallback |
| `38fba0e` | 17:13 | feat: use RainbowKit metaMaskWallet with MetaMask SDK |
| `91b5746` | 17:05 | fix: strict MetaMask detection + custom WalletConnect connector |
| `41f7aa4` | 16:39 | fix: base64 MetaMask icon, restore Rabby, use RainbowKit walletConnectWallet |
| `7c7465f` | 16:31 | fix: smart MetaMask wallet with injected/WalletConnect fallback |
| `0a532e0` | 16:22 | fix: use curated wallet list with injected connectors |
| `4d54e6b` | 16:10 | fix: add ssr:true and proper WagmiProvider SSR rendering |
| `d244c13` | 15:41 | fix: downgrade wagmi to v2 and add WalletConnect via getDefaultWallets |
| `0c266a2` | 15:22 | fix: revert to simple injected connectors, remove unused MetaMask SDK dep |
| `301e28f` | 14:56 | fix: install @metamask/connect-evm for MetaMask SDK connector |
| `32e1737` | 14:43 | fix: use connectorsForWallets so RainbowKit shows MetaMask on mobile |
| `5ac58d4` | 14:39 | fix: add RainbowKit wallets prop to show MetaMask on mobile platforms |
| `5020ee3` | 14:28 | fix: add fallback injected() connector for MetaMask mobile browser without EIP-6963 |
| `a85c664` | 14:10 | fix: move mobile drawer to portal to bypass Safari backdrop-filter trapping |
| `464ad90` | earlier | fix: mobile drawer z-index raised to avoid being covered by page content |

---

## 七、当前系统状态

### 前端

| 项目 | 值 |
|---|---|
| 在线地址 | https://football-betting-mu.vercel.app |
| GitHub | https://github.com/Yangzhou-Y/football-betting |
| 分支 | `fix/mobile-WalletConnect`（26 commits，尚未合并 main） |
| 框架 | Next.js 16.2.9 + React 19.2.4 + wagmi 2.19.5 + RainbowKit 2.2.11 |
| WalletConnect projectId | `c3ab3b19085ebe629d528e219ebd3546` |

### 钱包配置（最终状态）

| 钱包 | 实现方式 | 桌面行为 | 手机行为 |
|------|----------|----------|----------|
| MetaMask | `connectorsForWallets` + 自定义三级检测 | 有拓展→注入连接；无拓展→显示"获取" | MetaMask 浏览器→注入；其他→显示下载引导 |
| Rabby | `connectorsForWallets` + RainbowKit 内置 | 有拓展→注入连接 | — |
| WalletConnect | `connectorsForWallets` + `showQrModal: true` | wagmi 原生弹窗 QR 码 | 深链跳转钱包 App |

### 合约

| 项目 | 值 |
|---|---|
| 网络 | Conflux eSpace 测试网 (chainId=71) |
| RPC（钱包端） | `https://evmtestnet.confluxrpc.com` |
| RPC（浏览器端） | `/rpc/conflux-testnet` (Next.js rewrites 代理) |
| 合约地址 | `0x8A60409F40fEDFE7D07D61866757899F2fE35B63` |
| USDT 地址 | `0x7d682e65efc5c13bf4e394b8f376c48e6bae0355` |

---

## 八、最终结论 & 已知限制

### WalletConnect 桌面端 — MetaMask App 实验性功能限制

经过 30+ 次提交的排查，代码侧已做到极致：
- Core 单例初始化（零构建警告）
- RPC URL 使用公共 HTTPS 地址
- `showQrModal: true` 绕过 React 19 不兼容的 RainbowKit 弹窗
- F12 无任何报错
- 手机端 WalletConnect 深链跳转正常

桌面端扫码偶发不稳定的根因：**MetaMask App 的 WalletConnect 是实验性功能**（App 内标注为"Experimental"）。连接成功后 wagmi 状态不更新的原因在于 MetaMask WC 实现未正确回传 session 状态。该问题在 Trust Wallet、Rainbow 等成熟 WC 实现中不存在。

### 当前可行的钱包连接方案

| 平台 | 推荐方案 | 稳定性 |
|------|----------|--------|
| 桌面端（Chrome/Edge） | MetaMask 拓展 `injected` 直连 | 100% 稳定 |
| 手机端 | MetaMask App 内置浏览器访问 → `injected` 直连 | 100% 稳定 |
| 桌面端（无法装拓展） | WalletConnect 扫码（需多试几次） | 70% 稳定 |
| 手机端 Safari/Chrome | WalletConnect 深链跳转 | 正常 |
| 桌面端任意浏览器 | Rabby 拓展 `injected` 直连 | 正常 |

### 已知限制

1. **React 19 兼容性**：RainbowKit v2.2.11 内置 WalletConnect QR 弹窗组件在 React 19 下崩溃，必须用 `showQrModal: true` 绕过。

2. **MetaMask WC 实验性**：如上所述，非代码问题，等 MetaMask 转正。

3. **钱包签字显示 ETH 而非 CFX**：Conflux eSpace 使用 18 位小数，MetaMask 统一显示 "ETH"。实际扣费为 CFX。

4. **分支待合并**：`fix/mobile-WalletConnect` 建议 squash merge 到 main，保留 RPC 修复 + MetaMask 检测优化。
