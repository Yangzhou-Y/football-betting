# 开发日志 — 2026-06-26

## 项目：FootballBetting DApp 世界杯竞猜平台

> **今日主线**：代码审查（Claude Opus 4.8 + DeepSeek v4 Pro 双模型交叉审查）→ 审查修复 → 全项目注释 → 前端 UX 增强 → 新分支 `fix/frontend-update1`

---

## 一、代码审查 — 双模型交叉审查

### 1.1 审查流程

使用两个独立分支进行交叉验证：

| 分支 | 模型 | 角色 |
|------|------|------|
| `codeReview/wholeProject-ClaudeOpus4.8` | Claude Opus 4.8 | 主审查 + 修复实施 |
| `codeReview/wholeProject-deepseekV4pro` | DeepSeek v4 Pro | 独立审查（对照组） |

两个模型分别审查全项目代码（合约 + 脚本 + 前端），再由 Claude Opus 4.8 汇总两份审查报告，去重后实施修复。

### 1.2 审查发现的 Bug 分类统计

共发现并分类 21 个问题，按严重程度：

| 严重度 | 数量 | 状态 |
|--------|------|------|
| 严重 | 2 | 已修复 |
| 中危 | 3 | 已修复 |
| 轻微 | 11 | 已修复 |
| 非问题 | 5 | 不处理 |

---

## 二、代码审查修复 — `codeReview/wholeProject-ClaudeOpus4.8` 分支

### 2.1 Round 1: 小数位统一 + 下溢保护 + 测试修复（`0fcb890`）

**严重问题 #1：USDT 小数位数硬编码散布各处**

同一魔数 `6` 或 `18` 散落在 10+ 个文件中（合约注释、前端常量、脚本、测试）。如果未来更换 USDT 代币，需要逐文件查找修改，遗漏风险高。

**修复方案**：创建 `scripts/shared/usdt.ts` 作为 Hardhat 端的单一真相来源：

```ts
// scripts/shared/usdt.ts — USDT 小数位 & parseUnits 统一入口
export const USDT_DECIMALS = 18;
export const U = (amount: string) => parseUnits(amount, USDT_DECIMALS);
export const FU = (amount: bigint) => formatUnits(amount, USDT_DECIMALS);
```

所有脚本和测试文件改为从该文件导入，不再本地硬编码。

涉及文件：
- `scripts/shared/usdt.ts` — 新建
- `scripts/deploy.ts`、`diag.ts`、`interact.ts`、`test-bet.ts`、`test-create.ts` — 导入统一
- `test/FootballBetting.test.ts` — 删除本地 `U()` 定义，改为导入
- `contracts/FootballBetting.sol` — 注释更新为 18 位
- `contracts/MockERC20.sol` — 精确化币种对比表
- `frontend/src/lib/constants.ts` — 添加详细切换文档
- `frontend/src/lib/utils.ts` — 注释更新

切换 USDT 小数位只需改两处：`scripts/shared/usdt.ts` 和 `frontend/src/lib/constants.ts`。

**严重问题 #2：`_removeFromPool` 存在下溢风险**

合约 5 处 `pool -= amount` 放在 `unchecked` 块中（Gas 优化），若传入异常大的 `amount` 会静默下溢成极大值，导致奖池数据彻底损坏。

**修复方案**：5 处 `-= amount` 之前各加 `assert(pool >= amount)`，保留 `unchecked` 块但增加不变量断言。`assert` 失败时回退全部状态变更并退还 Gas，且不增加额外 Gas（`assert` 在满足时几乎零开销）。

**测试修复**：合约已改用 custom errors（Gas 优化 v4），但 32 处测试断言仍用旧版 `require` 字符串匹配，全部改为 `.to.be.revertedWithCustomError(contract, ErrorName)`。涉及 21 种 custom error 的对应关系映射。最终 65 pass / 0 fail。

**其他修复**：
- `diag.ts` / `test-create.ts` 的 `createMatch` 调用缺少 `matchName` 和 `allowDraw` 参数
- `interact.ts` 删除过时的硬编码注释
- 前端 `wagmi.ts` 加入 `hardhatLocal` 链，本地开发可直连

### 2.2 Round 2: 分页 + 修饰器重命名 + 重开退款（`34addf9`）

**Issue 5：合约层分页**

新增 `getMatchesPaginated(page, pageSize)` 函数：
- 返回 `(Match[] result, uint256 total)`，page 为 0-indexed
- 前端新增 `useMatchesPaginated` hook，赛事列表 12 条/页

**Issue 7：修饰器语义修正**

原代码混用 `onlyOwner`，实际允许 owner + admins 通过。修复后：
- `onlyAdmin`：允许 owner 和 admins（所有管理操作）
- `onlyOwner`：严格仅 owner（`addAdmin`/`removeAdmin`）

**Issue 4：重开赛事时手续费用可能不足**

`reopenMatch` 退还手续费时，如果 `platformBalance < fee` 会下溢。新增 `FeeRefundShortfall` 事件，记录 `(matchId, oldFee, available)`，差额可追溯但不可恢复。

**Issue 16：错误码歧义**

原 `DeadlineNotPassed` 在两个地方含义相反：
- `placeBet` 中：截止时间已过，无法投注
- `autoClose` 中：截止时间未到，无法关盘

新增 `DeadlineNotReached` 用于 `autoClose`，原 `DeadlineNotPassed` 仅用于 `placeBet`。

**注释修复**：3 处 `uint128` 上限从 `3.4e20` 更正为 `3.4e38`。

### 2.3 合约变更回退（`e77c22e`）

代码审查中合约层的修改（分页、修饰器、事件）全部回退，保留前端和脚本的修复。

原因：合约字节码不能变，所有功能增强必须通过前端实现。

### 2.4 文档更新

- `ca10506`：README 更新脚本目录列表、修正 custom error 数量（32 而不是 36）、移除已过时的 SSTORE 优化描述。删除前端未使用的 `RESULT_LABELS`（已被 `RESULT_KEYS + t()` i18n 替代）
- `696cb98`：README 新增 Project Highlights 板块
- `6334a07`：投注面板文案优化 — 新增无人猜中风险提示（`bet.noWinnerHint`）、重写切换投注提示（`confirm.switchHint` 澄清"以本次输入金额为准"）、中英双语同步

### 2.5 分支合并

`codeReview/wholeProject-ClaudeOpus4.8` → `main`（`7560eff`），包含上述全部修复。

---

## 三、全项目中文注释（`f573ff7`）

对项目 31 个文件补全了中文注释，共 884 行。注释风格为 JSDoc 风格块注释，重点解释 WHY 而非 WHAT。

### 涉及文件清单

**scripts/ — 6 个脚本**
| 文件 | 注释内容 |
|------|---------|
| `admin.ts` | 权限模型（owner vs admins）、用法示例、环境变量指南 |
| `check.ts` | 链上状态检查用途、输出字段解释 |
| `diag.ts` | Hardhat 时间偏移问题背景、诊断结论指南 |
| `fix-time.ts` | `evm_setNextBlockTimestamp` 原理与 RPC 方法说明 |
| `test-bet.ts` | 投注测试前置条件、`placeBet` 流程 |
| `test-create.ts` | 区块时间 vs 系统时间的区别与应对策略 |

**frontend/src/hooks/ — 6 个 Hook**
| 文件 | 注释内容 |
|------|---------|
| `useMatches.ts` | 数据流图、TanStack Query 缓存策略、`isReady` 守卫 |
| `useCancelBet.ts` | 合约行为说明、状态机流转、Gas 退款机制 |
| `useClaimReward.ts` | Parimutuel 公式、Pull 模式资金流、伪领奖机制 |
| `useIsAdmin.ts` | owner + admins 双重权限检查逻辑 |
| `useUserBets.ts` | 三个 Hook 分工、复杂度分析、previewReward vs claimReward |
| `useLeaderboard.ts` | 事件扫描流程、分片原理、缓存策略、排序规则 |

**frontend/src/lib/ — 3 个库文件**
| 文件 | 注释内容 |
|------|---------|
| `config.ts` | 多链支持机制、chainId 回退逻辑 |
| `refresh.ts` | 刷新 Context 原理、与 TanStack Query 的关系 |
| `i18n.tsx` | 翻译流程、设计取舍、添加新翻译的步骤 |

**frontend/src/components/ — 7 个组件**
| 文件 | 注释内容 |
|------|---------|
| `LayoutClient.tsx` | Context 层级结构图、各 Provider 职责 |
| `Navbar.tsx` | 响应式策略、权限感知、Portal 抽屉原理 |
| `MatchCard.tsx` | 显示内容清单、视觉设计、交互说明 |
| `TeamNameDisplay.tsx` | bytes32→队名→国旗的完整数据转换链 |
| `TxToast.tsx` | pending/success/error 三种类型、生命周期管理 |
| `ConfirmDialog.tsx` | Portal 渲染、可配置项、关闭方式 |
| `MatchStatusBadge.tsx` | 状态颜色映射、deadline 覆盖逻辑 |

**frontend/src/app/ — 8 个页面**
| 文件 | 注释内容 |
|------|---------|
| `layout.tsx` | 服务端壳→客户端核心分层、viewport 防缩放、字体配置 |
| `page.tsx` | 首页结构：三栏统计 + 热门赛事 + 即将开赛 |
| `matches/page.tsx` | 客户端分页策略、筛选与排序规则 |
| `matches/[id]/page.tsx` | 全流程交互、投注状态机、Toast 生命周期 |
| `my-bets/page.tsx` | 盈亏计算公式、响应式表格设计 |
| `admin/page.tsx` | 权限控制、比分两步确认、Portal 弹窗 |
| `leaderboard/page.tsx` | 排名算法、事件聚合来源、数据刷新机制 |
| `error.tsx` | Error Boundary 机制、reset 函数与 try-catch 互补 |

**frontend/src/providers/ — 1 个文件**
| 文件 | 注释内容 |
|------|---------|
| `Web3Provider.tsx` | Wagmi+RainbowKit+ReactQuery 技术栈关系、MetaMask 三级检测、Provider 嵌套结构 |

---

## 四、`fix/frontend-update1` 分支 — 前端 UX 增强

### 4.1 Parimutuel 赔率实时展示（`5fdcbc2`）

**背景**：用户投注时无法直观判断各选项的预期回报。合约使用 Parimutuel（同注分彩）模式，赔率由奖池实时决定。

**实现**：

1. 新增 `calcOdds()` 函数（`lib/utils.ts`）：
   ```ts
   export function calcOdds(optionPool: bigint, totalPool: bigint, feeRate: number): string | null {
     if (optionPool <= 0n || totalPool <= 0n) return null;
     const distributable = (totalPool * BigInt(10000 - feeRate)) / 10000n;
     const oddsX100 = (distributable * 100n) / optionPool;
     return (Number(oddsX100) / 100).toFixed(2);
   }
   ```
   赔率 = 可分配奖池 / 该选项奖池，保留两位小数。

2. 展示位置：
   - **赛事卡片**（`MatchCard`）：比例条下方显示三选项赔率（`grid-cols-3` 或 `grid-cols-2`）
   - **赛事详情页**（奖池栏）：每行右侧显示 `赔率 X.XX`
   - **逻辑**：奖池为 0 的选项显示 `-`，手续费后的可分配奖池自动计算

### 4.2 快捷金额按钮

投注面板金额输入框下方新增三个按钮：`1` / `10` / `100`（USDT），点击直接填入金额，减少手机端手动输入。

### 4.3 参与人数统计

**问题**：赛事卡片只显示奖池总额，用户不知道"有多少人参与了这场比赛"。

**实现**（新增 `useParticipantCounts` hook）：
- 扫描全量 `BetPlaced` 事件（100k 块分片）
- 按 `matchId` 聚合，对 `user` 去重计数
- 返回 `Map<matchId, 参与人数>`
- 缓存 60s，避免页面切换重复扫描

**显示位置**：赛事卡片奖池行 → `奖池: XXX USDT · N 人参与`

**已知限制**：取消投注不发事件，被计入为近似值。

### 4.4 我的竞猜分页

**问题**：同一用户投注 50 场以上时，单页加载全部记录导致性能下降。

**实现**：
- 每页 10 条，按 `matchId` 降序排列（新投注在前）
- 仅箭头 ←/→ 翻页，无跳页控件（保持 UI 简洁）
- 翻页时给表格/卡片列表加 `animate-page-enter` 动画
- `key={safePage}` 确保翻页触发入场动画

### 4.5 构建日志忽略

`.gitignore` 新增 `build.log`（`cbae67a`），避免临时构建日志被提交。

---

## 五、涉及文件（main）

| 文件 | 变更 |
|------|------|
| `scripts/shared/usdt.ts` | 新建，USDT 小数位统一入口 |
| `scripts/deploy.ts` | 导入 `U()` 替换本地 `parseUnits` |
| `scripts/diag.ts` | 补充 `matchName`/`allowDraw` 参数 |
| `scripts/interact.ts` | 删除过时硬编码注释 |
| `scripts/test-bet.ts` | 导入 `U()` |
| `scripts/test-create.ts` | 导入 `U()` + 补充参数 |
| `scripts/admin.ts` | 添加退出处理 `.then(exit).catch(exit)` |
| `test/FootballBetting.test.ts` | 32 处 custom error 断言修复 |
| `contracts/FootballBetting.sol` | 注释更新（未改字节码） |
| `contracts/MockERC20.sol` | 注释更新（未改字节码） |
| `frontend/src/lib/constants.ts` | USDT 切换文档 + 删除 RESULT_LABELS |
| `frontend/src/lib/utils.ts` | 注释更新 |
| `frontend/src/lib/wagmi.ts` | 加入 hardhatLocal |
| `frontend/src/lib/config.ts` | chainId 回退逻辑优化 |
| `frontend/src/lib/flags.ts` | flagOnError 隐藏破损图片 |
| `frontend/src/lib/i18n.tsx` | 投注文案优化 + 无人猜中提示 |
| `frontend/src/lib/refresh.ts` | 注释补全 |
| `frontend/src/app/matches/[id]/page.tsx` | 投注文案更新 |
| `frontend/src/components/shared/TeamNameDisplay.tsx` | flagOnError handler |
| `README.md` | Project Highlights + 脚本列表 + 错误数修正 |
| 31 个文件的注释补全 | 884 行中文注释 |

## 六、涉及文件（fix/frontend-update1，共 2 个 commit）

| 文件 | 变更 |
|------|------|
| `frontend/src/lib/utils.ts` | 新增 `calcOdds()` 赔率计算函数 |
| `frontend/src/lib/i18n.tsx` | 新增赔率/参与人数/快捷金额/翻页翻译 |
| `frontend/src/hooks/useParticipantCounts.ts` | **新建**，扫描 BetPlaced 事件统计参与人数 |
| `frontend/src/components/match/MatchCard.tsx` | 显示赔率 + 参与人数 + 注释 |
| `frontend/src/app/matches/[id]/page.tsx` | 快捷金额按钮 + 奖池赔率 + 注释 |
| `frontend/src/app/matches/page.tsx` | 参与人数传递 + 注释 |
| `frontend/src/app/page.tsx` | 参与人数传递 + 注释 |
| `frontend/src/app/my-bets/page.tsx` | 分页（10条/页）+ 注释 |
| `.gitignore` | 忽略 `build.log` |

---

## 七、Git 提交记录（2026-06-26）

| 提交 | 时间 | 分支 | 说明 |
|------|------|------|------|
| `cbae67a` | 17:47 | fix/frontend-update1 | chore: ignore temporary build.log |
| `5fdcbc2` | 17:44 | fix/frontend-update1 | feat(frontend): betting UX + parimutuel odds + pagination |
| `f573ff7` | 17:12 | main | docs: 为整个项目添加详细完整的中文注释（31 文件 / 884 行） |
| `7560eff` | 16:53 | main | merge: codeReview/wholeProject-ClaudeOpus4.8 |
| `6334a07` | 16:48 | codeReview/ClaudeOpus4.8 | fix(frontend): 优化投注提示文案 + 无人猜中风险提示 |
| `696cb98` | 16:07 | codeReview/ClaudeOpus4.8 | docs: add project highlights section to README |
| `ca10506` | 15:47 | codeReview/ClaudeOpus4.8 | docs: update README + remove deprecated RESULT_LABELS |
| `e77c22e` | 15:34 | codeReview/ClaudeOpus4.8 | revert: 合约变更回退（保留前端/脚本修复） |
| `34addf9` | 15:28 | codeReview/ClaudeOpus4.8 | code review round 2: pagination + modifier rename + reopen fix |
| `0fcb890` | 15:12 | codeReview/ClaudeOpus4.8 | code review round 1: unify 18 decimals + pool underflow + test fixes |
| `2d4b517` | 14:24 | main | merge: mobile UI fixes from fix/mobile-ui2 |

---

## 八、当前系统状态

### 分支

| 分支 | 状态 |
|------|------|
| `main` | 生产分支，已开启保护，已合并 codeReview 修复 + 全项目中文注释 |
| `fix/frontend-update1` | **当前分支**，前端 UX 增强（赔率/快捷金额/参与人数/分页），2 个 commit，已推送远程，待合并 |
| `codeReview/wholeProject-ClaudeOpus4.8` | 已合并到 main |
| `codeReview/wholeProject-deepseekV4pro` | 独立审查对照组，未合并（与 ClaudeOpus4.8 内容相同但无最新两个 commit） |
| `fix/mobile-WalletConnect` | 保留 WC 实验代码，待 MetaMask WC 转正 |
| `fix/mobile-ui2` | 已合并到 main |
| `fix/mobile-ui` | 已合并到 main |

### 合约

| 项目 | 值 |
|------|-----|
| 网络 | Conflux eSpace 测试网 (chainId=71) |
| RPC | `https://evmtestnet.confluxrpc.com` |
| 合约地址 | `0x8A60409F40fEDFE7D07D61866757899F2fE35B63` |
| USDT 地址 | `0x7d682e65efc5c13bf4e394b8f376c48e6bae0355` |
| USDT 小数位 | 18（Conflux 水龙头 USDT） |

### 前端

| 项目 | 值 |
|------|-----|
| 在线地址 | https://football-betting-mu.vercel.app |
| 框架 | Next.js 16.2.9 + React 19.2.4 + wagmi 3.6.16 + RainbowKit 2.2.11 |
| 钱包 | MetaMask（桌面拓展 + 手机 App 内置浏览器）、Rabby（桌面拓展） |
| 语言 | 中/英双语切换，客户端 Context 驱动 |

### 代码审查成果

| 指标 | 值 |
|------|-----|
| 发现问题总数 | 21 |
| 严重问题 | 2（已修复） |
| 中危问题 | 3（已修复） |
| 轻微问题 | 11（已修复） |
| 非问题 | 5（不处理） |
| 涉及文件 | 20 个 |
| 测试 | 65 pass / 0 fail |

---

## 九、经验总结

1. **双模型交叉审查**：DeepSeek v4 Pro 作为独立审查，提供了与 Claude Opus 不同的视角，两份报告汇总后有效避免了单一模型的盲区。

2. **USDT 小数位统一**：`scripts/shared/usdt.ts` 的单一真相来源模式值得在其他共享常量上推广。一个魔数散落在 10+ 个文件中的维护成本远超创建共享模块的成本。

3. **注释策略**：注释应解释 WHY（设计决策、边界条件、陷阱）而非 WHAT（代码本身自文档化）。本轮 884 行注释均遵循此原则。

4. **合约不可变约束**：由于合约已部署且字节码不能变，所有功能增强必须在前端实现。Parimutuel 赔率计算、参与人数统计都是合约层没有的数据，通过前端扫描事件派生。

---

## 十、Copilot 会话补充（收尾动作）

### 10.1 分支与交付动作

- 创建并切换工作分支：`fix/frontend-update1`
- 提交并推送前端增强改动：`5fdcbc2`
- 提交并推送 `.gitignore` 日志忽略规则：`cbae67a`
- 远程分支已建立并跟踪：`origin/fix/frontend-update1`

### 10.2 构建日志清理与忽略

- 说明并确认两个 `build.log` 为临时构建输出文件（非业务文件）
- 清理根目录与 `frontend` 目录下临时日志
- 在根 `.gitignore` 增加 `build.log` 规则，避免后续误入版本控制

### 10.3 交付状态

- 本次新增功能（快捷金额/赔率展示/参与人数/我的竞猜分页）均已提交并推送
- `fix/frontend-update1` 当前可直接发起 PR 合并到 `main`

---

## 十一、已知限制 / 待办

1. 赛事卡片赔率显示不明确
2. 赛事页面翻页按钮文本不合适
3. 未连接钱包时首页和赛事页面无信息
4. 将继续在fix/frontend-update1分支中进行修改，暂未合并进主分支